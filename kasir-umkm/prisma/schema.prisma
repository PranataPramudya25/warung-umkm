generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // Baris url dihapus dari sini
}

model product {
  id    Int     @id @default(autoincrement())
  name  String
  price Int
}